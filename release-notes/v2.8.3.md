## Updater hotfix

- Fixed launcher updates silently closing without installing when the existing launcher came from a multi-file deployment.
- The updater now runs from the newly downloaded self-contained launcher, so it no longer depends on files beside the old executable.
- Prevents the same update notification from reappearing after an apparently completed installation.
