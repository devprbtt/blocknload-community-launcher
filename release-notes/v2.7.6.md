## What's new

- Removed the legacy WinForms launcher project and made Avalonia the only launcher UI.
- Updated shipped recommended settings to the new exported baseline for launcher updates.

## Fixes

- Fixed Avalonia feature-setting persistence so launcher edits sync back into the active profile/runtime state.
- Removed dead import/export entries for launcher features that no longer exist in the current UI.

## Notes

- Launcher updates now continue to overwrite `recommended` while preserving each user's `personal` snapshot.
