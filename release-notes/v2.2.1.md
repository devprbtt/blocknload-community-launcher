# v2.2.1

## Fixes
- Fixes the external launcher refresh when a stale remembered bootstrap path exists from an earlier run.
- The installed launcher now scans recent bootstrap log entries and refreshes every older external launcher copy it can safely update.

